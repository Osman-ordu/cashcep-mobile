import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      width: '100%',
      marginBottom: 24,
    },
    track: {
      height: 3,
      borderRadius: 1.5,
      overflow: 'hidden',
    },
    progress: {
      height: '100%',
      borderRadius: 1.5,
    },
  });